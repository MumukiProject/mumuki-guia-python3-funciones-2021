---
rules:
- when:
    error_contains: IndentationError
  then: no olvides de tabular mediante la tecla  `Tab ↹` la última línea
- when:
    error_contains: "^"
  then: 'mirá el símbolo `^` :bulb: , ¿a dónde está apuntando? ¿Qué nos estará queriendo
    decir Python?'
