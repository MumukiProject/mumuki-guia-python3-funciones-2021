---
rules:
- when:
    these_expectations_failed:
    - "* Not:Uses:startswith"
  then: ":warning: en este ejercicio no necesitás `str.startswith`. Te la contamos
    sólo como ejemplo :wink:"
- when:
    these_tests_failed:
    - No falta contemplar el espacio
  then: Parece que no estás contemplando el espacio; la `longitud_nombre_completo`
    debe devolver `1` más que las longitudes sumadas de `nombre` y `apellido`
